
const config = {
  mapWidth: 50,
  mapHeight: 50,
  tileSize: 16,
  roomCreationAttempts: 1000,
  minRoomWidth: 5,
  maxRoomWidth: 18,
  minRoomHeight: 5,
  maxRoomHeight: 20,


};

export default config;